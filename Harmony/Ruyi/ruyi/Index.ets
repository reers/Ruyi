/**
 * @reers/ruyi — Harmony SVG load & render runtime.
 * NAPI bridge + ArkTS API live here; ThorVG C API comes from @vnixx/thorvg.
 */
export {
  Ruyi,
  RuyiOptions,
  GradientStop,
  GradientTint,
  RuyiOptionsInit,
  RuyiSvgSource,
} from './src/main/ets/ruyi/Ruyi';
